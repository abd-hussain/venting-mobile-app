name: venting_lint
description: "Lint rules and code style guidelines for Venting projects"
publish_to: "none"
version: 1.0.0

environment:
  sdk: '>=3.9.2 <4.0.0'

resolution: workspace

dependencies:
  leancode_lint: ^19.0.1

dev_dependencies:
  dependency_validator: 5.0.5
  test: ^1.25.0
  yaml: ^3.1.2