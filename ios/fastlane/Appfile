# Overridden by CI/Makefile via BUNDLE_ID (prod vs prod+.dev).
app_identifier(ENV["BUNDLE_ID"] || "com.vent.ventingMobileApp")

apple_id("abd.alhaj.hussain@gmail.com") # Apple Developer Portal username

itc_team_id("108758801") # App Store Connect Team ID
# Developer Portal Team ID — must match DEVELOPMENT_TEAM in ios/Runner.xcodeproj
team_id("DX9H87DTX5")

# For more information about the Appfile, see:
#     https://docs.fastlane.tools/advanced/#appfile
