# Private Match certificates repo for Venting (create + bootstrap before CI readonly mode).
# Contains App Store certs/profiles for both flavors:
#   com.vent.ventingMobileApp
#   com.vent.ventingMobileApp.dev
git_url("https://github.com/abd-hussain/Venting-Certificates")

storage_mode("git")
git_branch("main")

type("appstore")
app_identifier(["com.vent.ventingMobileApp", "com.vent.ventingMobileApp.dev"])

username("aboud.masoud.92@gmail.com") # Apple Developer Portal username

# For all available options run `fastlane match --help`
# Docs: https://docs.fastlane.tools/actions/match
